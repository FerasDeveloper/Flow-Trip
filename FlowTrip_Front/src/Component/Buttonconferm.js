import React from "react";
import "./Buttonconferm.css";

const Button = ({ onClick }) => {
  return (
    <button className="buttonconferm" onClick={onClick}>
    </button>
  );
};

export default Button;
